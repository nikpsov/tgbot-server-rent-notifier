version: "3.8"

services:
  bot:
    build: .
    container_name: server_pay_bot
    restart: unless-stopped
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      OWNER_CHAT_ID: ${OWNER_CHAT_ID}
      TZ: ${TZ:-Europe/Moscow}
    volumes:
      - ./data:/app/data
